# Спецификация: Редактор пользовательских армлистов

**Дата:** 2026-03-15
**Статус:** Утверждено (после ревью)
**Автор:** Claude

---

## Контекст

Пользователи хотят создавать собственные армлисты для игры Бронепехота: балансировать юнитов, добавлять новые фракции и делиться своими наборами с сообществом. Сейчас все источники захардкожены в `sources-registry.ts` и требуют изменений в коде проекта.

**Проблема:** Нет возможности создавать кастомные армлисты без участия разработчиков.

**Решение:** Отдельное приложение-редактор по адресу `/editor` с хранением данных в localStorage и возможностью экспорта для публикации.

---

## Ключевые решения

| Решение | Выбор | Обоснование |
|---------|-------|-------------|
| Модель расширения | Гибридная | Можно создать источник с нуля ИЛИ на основе существующего |
| Изображения | URL + превью | Пользователь указывает URL, изображение загружается везде |
| Layout редактора | Адаптивный | Десктоп: 3-колоночный сайдбар. Мобильный: табы + bottom sheet |
| Интеграция с /app | Раздел "Мои источники" | Пользовательские источники отдельно от встроенных |
| Импорт/Экспорт | JSON + QR-код | Файлы для офлайн + QR для мобильного шаринга |
| Валидация | Стандартная + подсказки | Предупреждения о проблемах, но можно сохранить |
| Хранение | localStorage + abstraction | Обёртка для будущего перехода на Firestore |

---

## Архитектура

### Структура приложения

```
/editor                    — Главная страница редактора
  ├── Sidebar (3 колонки)
  │   ├── SourcesList      — Список источников
  │   ├── FactionsList     — Фракции выбранного источника
  │   └── UnitsList        — Юниты выбранной фракции
  ├── MainEditor           — Форма редактирования юнита
  └── Modals
      ├── PreviewModal     — Превью карточки юнита
      ├── ExportModal      — Экспорт JSON/QR
      ├── ImportModal      — Импорт файла/кода
      └── CreateSourceModal — Создание источника
```

### Модель данных

```typescript
interface CustomSource {
  id: string;                    // Уникальный ID: auto-generated "custom_" + nanoid(8)
  name: string;                  // Название (ru)
  description: string;           // Описание
  version: string;               // Версия (1.0 по умолчанию)
  baseSource: SourceID | null;   // Базовый источник для расширения (null = с нуля)
  baseSourceVersion: string;     // Версия базового источника (для предупреждений о несовместимости)
  factions: CustomFaction[];     // Фракции (упрощённый интерфейс)
  squads: Squad[];               // Отряды
  machines: Machine[];           // Техника
  createdAt: string;             // ISO дата создания
  updatedAt: string;             // ISO дата обновления
}

// Упрощённый интерфейс фракции для пользовательских источников
interface CustomFaction {
  id: string;                    // ID фракции (slug)
  name: string;                  // Название
  color: string;                 // Hex цвет
  description?: string;          // Опциональное описание
}
```

### Генерация ID

**Источники:** `custom_` + nanoid(8), например: `custom_x7Kp2mNq`

**Юниты:** `{faction_id}_custom_{slugified_name}`, например: `polaris_custom_elite_snipers`

**Функция генерации:**
```typescript
import { nanoid } from 'nanoid';

function generateSourceId(): string {
  return `custom_${nanoid(8)}`;
}

function generateUnitId(factionId: string, name: string): string {
  const slug = slugify(name, { lower: true, strict: true });
  return `${factionId}_custom_${slug}`;
}
```

### Storage Abstraction

```typescript
// src/lib/editor/storage.ts
interface CustomSourcesStorage {
  getAll(): CustomSource[];
  getById(id: string): CustomSource | null;
  save(source: CustomSource): void;
  delete(id: string): void;
  exportToJson(source: CustomSource): string;
  importFromJson(json: string): CustomSource;
}

// Текущая реализация: localStorage
// Будущее: Firestore adapter
```

### Константы

```typescript
// src/lib/constants.ts — добавить
export const LOCAL_STORAGE_KEYS = {
  // ... существующие ключи
  CUSTOM_SOURCES: 'bronepehota_custom_sources',
} as const;
```

### QR-код: ограничения

QR-коды ограничены по ёмкости (~3 КБ для максимального размера). Стратегия:

1. **Маленькие источники (до 10 юнитов):** QR содержит полный JSON
2. **Большие источники:** QR недоступен, показываем сообщение "Для больших источников используйте JSON файл"

```typescript
const QR_SIZE_LIMIT = 3000; // байт

function canUseQrCode(source: CustomSource): boolean {
  const json = JSON.stringify(source);
  return json.length <= QR_SIZE_LIMIT;
}
```

### Интеграция с sources-registry

```typescript
// src/lib/sources-registry.ts
// Добавить функцию:
function getAllSourcesWithCustom(): SourceData[] {
  const builtIn = getAllSources();
  const custom = customSourcesStorage.getAll().map(convertToSourceData);
  return [...builtIn, ...custom];
}
```

### Переопределение юнитов

При расширении источника (`baseSource` не null):
- Юнит с новым ID → **добавляется** к фракции
- Юнит с существующим ID → **полностью заменяет** оригинал (частичное переопределение не поддерживается)
- Фракция с новым ID → **добавляется** к источнику

**Примечание:** Для частичного изменения (например, только стоимость) пользователь должен скопировать оригинального юнита и изменить нужные поля. В UI есть кнопка "Клонировать" для этого.

### Энциклопедия и лор

Пользовательские источники **не поддерживают** энциклопедические данные (lore, history, tactics). Причины:
1. Энциклопедия — централизованная база знаний проекта
2. Пользовательские источники фокусируются на игровых данных

Если пользовательский юнит переопределяет оригинальный, он **сохраняет** связь с энциклопедией по ID. Если ID новый — энциклопедии нет.

---

## UI Компоненты

### Главная страница (/editor)

**Layout:** 3-колоночный сайдбар слева + основная область справа

```
┌─────────────────────────────────────────────────────────────────────┐
│  [Источники]  │  [Фракции]    │  [Юниты]     │  Редактор юнита      │
│  ──────────── │  ──────────── │  ───────────  │  ─────────────────── │
│  + Новый      │  + Добавить   │  Отряды|Техн. │  Название: [____]    │
│  ──────────── │  ──────────── │  + Новый      │  Стоимость: [___]    │
│  🔧 МойБаланс │  🔴 Полярис   │  ───────────  │  URL: [__________]   │
│  🔧 Турнир    │  🔵 Протек.   │  ✏️ Линейные  │  ─────────────────── │
│  🔧 Тестовый  │  🟡 Наемники  │    Штурмовики │  Солдаты:            │
│               │               │  ★ Снайперы   │  [Таблица солдат]    │
│  ──────────── │               │    Инженеры   │                      │
│  ⚠️ Локально  │               │               │  [Превью] [Сохранить]│
└─────────────────────────────────────────────────────────────────────┘
```

**Индикаторы в списке юнитов:**
- `✏️` — изменённый юнит (переопределяет базовый)
- `★` — новый юнит (добавлен пользователем)

**Напоминание внизу сайдбара:**
```
⚠️ Данные хранятся локально
Экспортируйте для публикации
```

### Мобильный Layout (< 768px)

На мобильных устройствах 3-колоночный сайдбар заменяется на табы + bottom sheet:

```
┌─────────────────────────────────────┐
│  Редактор армлистов         [⚙️]   │
│  ─────────────────────────────────  │
│                                     │
│  Выбран: 🔧 Мой Баланс              │
│  ─────────────────────────────────  │
│  ┌────────────────────────────────┐ │
│  │  🔴 Полярис              [>]   │ │
│  │  5 отрядов • 3 машины          │ │
│  └────────────────────────────────┘ │
│  ┌────────────────────────────────┐ │
│  │  🔵 Протекторат          [>]   │ │
│  │  4 отряда • 2 машины           │ │
│  └────────────────────────────────┘ │
│  ┌────────────────────────────────┐ │
│  │  🟡 Наемники             [>]   │ │
│  │  2 отряда • 1 машина           │ │
│  └────────────────────────────────┘ │
│                                     │
│  ⚠️ Данные локальные                │
├─────────────────────────────────────┤
│ [Источники] [Фракции] [Юниты] [+Добавить] │
└─────────────────────────────────────┘
```

**Редактирование юнита на мобильном:**
- Открывается как bottom sheet (свайп вниз для закрытия)
- Полноэкранная форма с прокруткой
- Кнопка "Превью" открывает карточку в модальном окне

**Брейкпоинты:**
- `< 768px` — мобильный layout (табы)
- `>= 768px` — десктоп layout (3-колоночный сайдбар)

### Модальное окно: Превью

Отображает карточку юнита в том же виде, как в основном приложении `/app`.

```
┌─────────────────────────────┐
│  [Изображение 280x140]      │
│  ─────────────────────────  │
│  Линейные клоны-пехота  [85]│
│  Полярис                    │
│  ─────────────────────────  │
│  [Ранг:5][Скор:4][Дал:D12]  │
│  4 солдата • Сила: 1D6      │
│  ─────────────────────────  │
│  ✏️ Переопределяет Star Sys │
│            [Закрыть]        │
└─────────────────────────────┘
```

### Модальное окно: Экспорт

```
┌─────────────────────────────────────┐
│  Экспорт: Мой Баланс                │
│  ─────────────────────────────────  │
│  ┌─────────────┐  ┌─────────────┐   │
│  │     📄      │  │     📱      │   │
│  │  JSON файл  │  │   QR-код*   │   │
│  │  [Скачать]  │  │ [Показать]  │   │
│  └─────────────┘  └─────────────┘   │
│  *QR до 10 юнитов                   │
│  ─────────────────────────────────  │
│  💡 Для публикации в проект:        │
│  1. Скачайте JSON файл              │
│  2. Создайте issue на GitHub        │
└─────────────────────────────────────┘
```

### Модальное окно: Импорт

```
┌─────────────────────────────────────┐
│  Импорт                             │
│  ─────────────────────────────────  │
│  ┌───────────────────────────────┐  │
│  │      📂                       │  │
│  │  Перетащите JSON файл сюда    │  │
│  │  или нажмите для выбора       │  │
│  └───────────────────────────────┘  │
│              — или —                │
│  [Вставьте код...        ] [Импорт]│
└─────────────────────────────────────┘
```

### Модальное окно: Создание источника

```
┌─────────────────────────────────────┐
│  Новый источник                     │
│  ─────────────────────────────────  │
│  Название источника *               │
│  [________________________]         │
│                                     │
│  Описание                           │
│  [________________________]         │
│                                     │
│  Тип источника                      │
│  ┌─────────────┐  ┌─────────────┐   │
│  │ ◉ С нуля    │  │ ○ На основе │   │
│  └─────────────┘  └─────────────┘   │
│                                     │
│  Базовый источник (если "На основе")│
│  [⭐ Star System         ▼]        │
│                                     │
│         [Отмена]  [Создать]         │
└─────────────────────────────────────┘
```

---

## Валидация данных

### Обязательные поля

**Отряд (Squad):**
- `id` — уникальный идентификатор
- `name` — название на русском
- `faction` — ID фракции
- `cost` — стоимость (число > 0)
- `soldiers` — массив 1-6 солдат

**Солдат (Soldier):**
- `rank` — ранг (0-7)
- `speed` — скорость (1-6)
- `range` — дальность (dice notation или "")
- `power` — сила (dice notation или "")
- `melee` — ближний бой (0-7)
- `armor` — броня (1-6)

**Техника (Machine):**
- `id`, `name`, `faction`, `cost` — как у отряда
- `rank` — ранг техники (1-5)
- `fire_rate` — скорострельность
- `ammo_max` — запас боеприпасов
- `durability_max` — максимальная прочность
- `weapons` — массив оружия
- `speed_sectors` — секторы скорости (должны покрывать 1..durability_max)

**Оружие (Weapon):**
- `name` — название
- `range` — дальность (dice notation)
- `power` — сила (dice notation)
- `ammo` — опционально: боеприпасы для конкретного оружия
- `special` — опционально: спецэффект

### Dice Notation

Допустимые форматы для `range` и `power`:
- `D6`, `D12`, `D20`
- `D6+2`, `D12-1`
- `2D6`, `2D12`
- `ББ` — только для melee
- `""` — пусто (нет стрельбы/ближнего боя)

### Предупреждения (не блокируют сохранение)

- Некорректный dice notation (например, `2D` вместо `2D6`)
- Gap в speed_sectors
- Отряд без солдат
- Дублирующийся ID юнита
- URL изображения не начинается с `http`

---

## Интеграция с основным приложением

### SourceSelector

Добавить раздел "Мои источники" после встроенных:

```tsx
// src/components/rules/SourceSelector.tsx
<div className="border-t border-dashed border-slate-600 my-4">
  <span className="text-slate-500 text-sm">▼ Мои источники</span>
</div>
{customSources.map(source => (
  <SourceCard
    key={source.id}
    source={source}
    isCustom={true}
  />
))}
```

### sources-registry.ts

```typescript
export function getSourceWithCustom(sourceId: string): SourceData | null {
  // Сначала проверяем встроенные
  if (sourcesRegistry[sourceId]) {
    return sourcesRegistry[sourceId];
  }

  // Затем пользовательские
  const customSource = customSourcesStorage.getById(sourceId);
  if (customSource) {
    return convertToSourceData(customSource);
  }

  return null;
}

export function getAllSourcesWithCustom(): ArmyListSource[] {
  const builtIn = getAllSources();
  const custom = customSourcesStorage.getAll().map(s => ({
    id: s.id,
    name: s.name,
    description: s.description,
    version: s.version,
  }));
  return [...builtIn, ...custom];
}
```

---

## Файловая структура

```
src/
├── app/
│   └── editor/
│       ├── layout.tsx           — Layout редактора
│       └── page.tsx             — Главная страница
├── components/
│   └── editor/
│       ├── EditorLayout.tsx     — Основной layout с сайдбаром (десктоп)
│       ├── MobileEditorLayout.tsx — Мобильный layout с табами
│       ├── SourcesList.tsx      — Список источников
│       ├── FactionsList.tsx     — Список фракций
│       ├── UnitsList.tsx        — Список юнитов
│       ├── SquadEditor.tsx      — Редактор отряда
│       ├── MachineEditor.tsx    — Редактор техники
│       ├── SoldiersTable.tsx    — Таблица солдат
│       ├── PreviewModal.tsx     — Модалка превью
│       ├── ExportModal.tsx      — Модалка экспорта
│       ├── ImportModal.tsx      — Модалка импорта
│       ├── CreateSourceModal.tsx— Модалка создания источника
│       └── ValidationWarning.tsx— Компонент предупреждения
├── lib/
│   ├── constants.ts             — Добавить CUSTOM_SOURCES key
│   └── editor/
│       ├── storage.ts           — Storage abstraction
│       ├── validation.ts        — Функции валидации
│       ├── converters.ts        — Конвертация CustomSource ↔ SourceData
│       ├── qr-code.ts           — Генерация QR (с проверкой размера)
│       ├── id-generator.ts      — Генерация ID (nanoid)
│       └── types.ts             — Типы редактора
└── hooks/
    └── useEditorState.ts        — State management для редактора
```

### Новые зависимости

```json
{
  "dependencies": {
    "nanoid": "^5.0.0",
    "qrcode": "^1.5.0"
  }
}
```

---

## План тестирования

### Unit тесты

1. **validation.ts**
   - Валидные/невалидные dice notation
   - Проверка coverage speed_sectors
   - Обязательные поля отряда/техники

2. **storage.ts**
   - CRUD операции
   - Import/export JSON
   - Обработка ошибок

3. **converters.ts**
   - Конвертация CustomSource → SourceData
   - Мерж с базовым источником
   - Переопределение юнитов

### E2E тесты

1. **Создание источника**
   - Создать источник с нуля
   - Создать на основе Star System
   - Проверить появление в /app

2. **Редактирование юнита**
   - Создать новый отряд
   - Изменить существующий отряд
   - Проверить превью

3. **Импорт/Экспорт**
   - Экспорт в JSON
   - Импорт из файла
   - Импорт по QR-коду

4. **Интеграция с /app**
   - Выбор пользовательского источника
   - Добавление юнитов в армию
   - Проверка переопределений

---

## Открытые вопросы

1. **Максимальный размер localStorage?** — Браузеры ограничивают ~5-10 МБ. Для JSON данных этого достаточно, но Base64 изображения могут быстро заполнить лимит. Решение: не хранить изображения, только URL.

2. **Версионирование источников?** — При обновлении базового источника (Star System) пользовательские переопределения могут сломаться. Решение: хранить версию базового источника и предупреждать о несовпадении.

3. **Дубликаты при импорте?** — Что делать если импортируется источник с ID, который уже существует? Решение: показать диалог "Заменить / Импортировать как копию".

---

## Резюме

Редактор армлистов — отдельное десктопное приложение по `/editor` с:
- Гибридной моделью источников (с нуля или на основе существующего)
- 3-колоночным сайдбаром для навигации
- Превью карточек как в основном приложении
- Экспортом в JSON и QR-код
- Интеграцией с основным приложением через раздел "Мои источники"
- Абстракцией хранения для будущего Firestore
