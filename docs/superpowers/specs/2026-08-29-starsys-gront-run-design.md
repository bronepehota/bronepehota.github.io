# Импорт «СтарСис» 2001: Гронт, Рун, Мидгаард — дизайн

Дата: 2026-08-29. Ветка: `feat/starsys-gront-run-lore` (от origin/main).

## Источник

13 фото страниц (`~/Documents/new-book/`, рабочие копии `/tmp/new-book-small/`) —
фрагмент издания «Технолога» (переиздание наборов 2001 г. серии «СтарСис»):
повести «Схватка на Гронте» и «Вторжение на Рун» (завязки), кодекс-страницы
(Ульфхеднары, шагоход «Йотун» + штурмовая модификация, танк «Гарм», Снежные
Волки), двухстраничный вкладыш-реткон «Пояснение к наборам».

- Транскрипция: `.superpowers/sdd/new-book-text.md` (45К, дословная).
- Конспект/разложение: `.superpowers/sdd/new-book-outline.md`.
- **Атрибуция**: официальный «Технолог» → `loreAuthor: tehnolog`, кредит
  `{work: "Наборы «СтарСис»: «Схватка на Гронте» и «Вторжение на Рун» (2001)"}` без автора
  (авторы повестей не установлены — паттерн «Летописи»). Без АВБ.
- **Реткон-мост** (вкладыш): события = середина 45 века = Первая волна 4451;
  Детокс → Империя Полярис; Лига Свободных Планет → Торговый Протекторат
  (4102); на Руне/Гронте клон-пехота, ЗИДоксы = «ошибка разведки». Новый текст
  пишется в терминах НОВОЙ редакции (Полярис/Протекторат), старые имена
  упоминаются как исторические.

## Куда переносим

1. **Кампания** `src/content/campaigns/pervaya-volna-gront-i-rum.md`:
   title «Первая волна: Гронт и Рун», era "4451", order 5 (после «Имперских
   войн»), factions [polaris, protectorate, snow_wolves]; units из существующих:
   ulfhednary, polaris_lineynaya_klon_pehota (+ уместные существующие). Тело:
   адаптация обеих повестей (анти-копипаст ≤11 слов) + два игровых сценария
   (миссии из текста: Гронт — пульт самоуничтожения до высадки клон-пехоты
   Харма; Рун — оборона 4–5 дней до эсминца, отрезать десант от крейсера и
   космоплана, захватить боезапас) + абзац о «Гарме»/«Йотуне» как технике
   Мидгаарда. Кредит frontmatter (credit + loreAuthor tehnolog).
2. **Фракция snow_wolves** (`src/data/encyclopedia/factions.json`):
   description/shortDescription дополнить — герб (планета Мидгаард + трикватр +
   три волка = три капитана/флота), се-конунг Брейг Ульфсон и клятва вернуть
   Мидгаард, патрули буферной зоны по найму Протектората, готовность ко Второй
   волне. Кредит добавить массивом к существующему (creditList).
3. **Отряд snow_wolves_ulfhednary** (`src/data/encyclopedia/units/snow_wolves/squads.json`):
   lore/tactics дополнить кодексом — тренировки воли и боли, прыжковые ранцы
   (реактивный разгонный движитель: сближение, удар, отход), тактика страха.
4. **Реестр** `docs/ENCYCLOPEDIA_LORE_SOURCES.md`: запись №10 (источник,
   инвентаризация 13 страниц по блокам, реткон-мост); в «Очередь» — страницы о
   Марке Балларде (попросить владельца сфотографировать оставшуюся часть
   книги), «Летопись Звёздного Бытия» (кандидат), расхождения: Леан/Леана,
   Мидгард/Мидгаард, календарь 10577 ↔ 45 век (решается вкладышем), десант ↔
   клон-пехота (решено вкладышем — отметить).

## Границы (осознанно НЕ делаем)

- **Юнит-записи «Гарм»/«Йотун» не создаём**: в кодексе нет игровых статов
  (ранг/прочность/оружие/стоимость) — выдумывать не будем; machines.json
  snow_wolves дождётся карточек. Лор машин живёт в кампании и фракции.
- Герой Брейг Ульфсон — упоминанием во фракции; юнит-карточка при появлении статов.
- Полную «Летопись Звёздного Бытия» не трогаем (нет источника).

## Проверки

- `campaigns.test.ts` — счётчик кампаний +1, порядок/эра.
- `lore-credits-avb.test.ts` — новый work остаётся tehnolog (без АВБ).
- Анти-копипаст ≤11 против `new-book-text.md` (адаптация, не копия).
- `npm run validate` зелёной; e2e не требуется (новых UI-поверхностей нет —
  кампания рендерится существующей страницей; при желании smoke по статику).
